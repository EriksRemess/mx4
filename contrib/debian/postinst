#!/bin/sh

set -e

# Make the packaged HID access rule effective without requiring a reboot.
if [ "$1" = "configure" ] && command -v udevadm >/dev/null 2>&1; then
    udevadm control --reload-rules || true
    udevadm trigger --subsystem-match=hidraw || true
fi

exit 0
